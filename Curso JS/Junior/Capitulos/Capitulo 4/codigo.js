//METODOS DE STIRNG
// let cadena = 50;
// let cadena2 = ""

// let resultado = cadena.toString()

// document.write(resultado * 2)

// - concat() - junta dos o mas cadenas y retorna una nueva
// - startswith() - si una cadena comienza con los caracteres de otra cadena, devuelve true, sino devuelve
// - endswith() - si una cadena termina con los caracteres de otra cadena, devuelve true, sino devuelve fa.
// - includes() - si una cadena puede encontrarse dentro de otra cadena, devuelve true, sino devuelve false
// - indexof() - devuelve el indice del primer caracter de la cadena, si no existe, devuelve - 1
// - lastIndexof() - devuelve el ultimo indice del primer caracter de la cadena, si no existe, devuelve -1
// 
// - padstart() [propuesta de Estandar] - Rellenar cadena al principio con los caracteres deseados.
// - padEnd() [propuesta de ECMA] - Rellenar cadena al final con los caracteres deseados.
// - repeat() - Devuelve la misma cadena pero repetida la cantidad que le indiquemos.
// 
// - split() - Divide la cadena como le pidamos
// - substring() - Nos retorna un pedazo de la cena que seleccionamos
// - toLowerCase( ) - Convierte una cadena a minúscula
// - toUpperCase( ) - Convierte una cadena a mayúscula
// - toString() - método devuelve una cadena que representa al objeto especificado.
// - trim() - elimina los espacios en blanco al principio y al final de una cadena.
// - trimEnd() - elimina los espacios en blanco al final de una cadena.
// - trimstart() - elimina los espacios en blanco al comienzo de una cadena.
// - valueof() - retorna el valor primitivo de un objeto string

// Metodos de Arrays

// let numeros =["abecedario","manzana","pedro","dedo","bobo"];

// document.write(numeros + "<br>")

// let resultado = numeros.slice(0,);

// document.write(resultado)


// Transformadores
// - pop() - elimina el último elemento de un array y lo devuelve.
// shift() - elimina el primer elemento de un array y lo devuelve.
// - push() - agrega un elemento al array al final de la lista.
// reverse() - invierte el orden de los elementos de un array.
// - unshift() - agrega uno o más elementos al inicio del array, y devuelve la nueva longitud del array.
// sort() - ordena los elementos de unarreglo (array) localmente y devuelve el arreglo ordenado.
// splice() - cambia el contenido de un array eliminando elementos existentes y/o agregando nuevos elementos.

// Accesores
// join() - une todos los elementos de una matriz (u objeto similar) en una cadena y la devuelve.
// - slice() - devuelve una parte del array dentro de un nuevo array empezando por inicio hasta fin (fin no incluido).
// - Metodos ya vistos en cadenas: tostring(), indexof(), lastIndexof(), includes()

// De Repetición
// - filter() - crea un nuevo array con todos los elementos que cumplan la condición.
// forEach() - ejecuta la función indicada una vez por cada elemento del array.

// let numeros =["abecedario","manzana","pedro","dedo","bobo"];

// resultado = numeros.filter(numero => numero.length > 5)

// document.write(resultado)

// Objeto Math - Básico
// METODOS
// - sqrt() - Devuelve la raíz cuadrada positiva de un número.
// - cbrt() - Devuelve la raiz cúbica de un número.
// max( ) - Devuelve el mayor de crro o más números.
// min() - Devuelve el más pequeño de cero o más números.
// - random ( ) - Devuelve un número pseudo-aleatorio entre 0 y 1.
// round() - Devuelve el valor de un número redondeado al número entero aás cercano.
// - fround() - Devuelve la representación flotante de precisión simple aás cercana de un número.
// - floor() - Devuelve el mayor entero menor que o igual a un número.
// - trunc() - Devuelve la parte entera del número x, la eliminación de los dígitos fraccionarios.

// - PROPIEDADES
// - PI
// - SORT1_2  
// - SQRT2

// - E
// _ LN2
// -LN10
// -LOGZE
// - LOG10E

numero = Math.LN2;

document.write(numero)


